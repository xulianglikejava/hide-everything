name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

# Run JS actions on Node 24. Covers softprops/action-gh-release@v2, whose v2 tag
# still ships a Node 20 runtime (Node 20 is removed from runners 2026-09-16).
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore

      - name: Publish (single-file, self-contained)
        run: >
          dotnet publish -c Release -r win-x64 --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -o publish

      - name: Upload build artifact
        uses: actions/upload-artifact@v6
        with:
          name: HideEverything
          path: |
            publish/HideEverything.exe
            publish/LICENSE
            publish/THIRD-PARTY-NOTICES.md

      - name: Generate SHA256SUMS
        if: startsWith(github.ref, 'refs/tags/')
        shell: pwsh
        run: |
          $h = (Get-FileHash publish/HideEverything.exe -Algorithm SHA256).Hash.ToLower()
          "$h  HideEverything.exe" | Out-File publish/SHA256SUMS.txt -Encoding ascii -NoNewline

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            publish/HideEverything.exe
            publish/SHA256SUMS.txt
            publish/LICENSE
            publish/THIRD-PARTY-NOTICES.md
          generate_release_notes: true
