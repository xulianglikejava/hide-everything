# Windows 窗口隐藏工具

这是一个面向 Windows 桌面用户的老板键工具：用户可以把正在运行的程序或具体窗口加入控制范围，并通过全局快捷键在屏幕上隐藏或重新展示，同时保持目标程序继续运行。

## 语言

**老板键**：
用户在任意应用中都能触发的隐藏/展示快捷键。
_避免_：局部快捷键、窗口内快捷键

**受控程序**：
用户希望长期保存并按进程维度控制的桌面应用。
_避免_：目标进程、被隐藏程序

**受控窗口**：
桌面上可被单独识别和控制的顶层应用窗口；同一受控程序可以同时拥有多个受控窗口。
_避免_：窗口句柄、窗口实例

**隐藏**：
让窗口从用户当前可见的桌面界面中消失，但不结束或暂停其所属程序。
_避免_：关闭、退出、暂停、最小化

**持久配置**：
工具重启、目标程序重启后仍然保留并重新生效的用户设置。
_避免_：只在本次运行有效

**窗口匹配规则**：
用于在不同运行会话中重新识别受控窗口的稳定条件，例如程序路径、进程名、窗口类名和标题匹配方式。
_避免_：保存窗口句柄

**会话绑定**：
只针对当前打开的受控窗口生效的快捷键绑定；窗口关闭或工具重启后自然失效。
_避免_：永久窗口配置

**进程组切换**：
按受控程序触发老板键时，将该程序当前所有符合条件的真实顶层窗口作为一个整体隐藏或展示。
_避免_：只切换当前活动窗口

**桌面隐藏**：
受控窗口从屏幕、任务栏和 Alt+Tab 中消失，但其所属程序仍保持运行。
_避免_：仅最小化到任务栏

**恢复**：
将同一进程组的受控窗口重新展示，并尽量把隐藏前处于前台的窗口交还给用户焦点。
_避免_：强制最大化、改变窗口位置、无条件抢占其他窗口焦点

**后台驻留**：
工具在 Windows 登录后可选地静默启动到托盘，不显示主窗口但继续维持老板键和配置管理能力。
_避免_：每次登录都弹出设置窗口

**窗口级持久匹配**：
使用程序路径、窗口类名和可选的标题包含文本，在新的运行会话中重新识别同类受控窗口。
_避免_：把当前窗口句柄当作永久配置

**权限级别**：
工具默认以当前用户的普通权限运行；控制更高权限的目标程序时，需要用户显式提升工具权限。
_避免_：默认管理员运行、绕过 Windows 权限隔离

**切换快捷键**：
同一个全局快捷键在隐藏和展示状态之间切换目标，不区分单独的隐藏键和展示键。
_避免_：必须使用两组快捷键

**动态匹配**：
受控程序未运行或窗口重建时，持久规则和全局快捷键继续保留；新的符合条件的窗口出现后可以重新纳入控制范围。
_避免_：程序重启后必须重新配置

**后台运行不变**：
隐藏只改变窗口的可见性和桌面入口，不改变目标程序的进程、声音、网络或计算行为。
_避免_：隐藏即暂停、静音或限速

**动态窗口纳入**：
在进程组已隐藏时，新建且符合持久匹配规则的真实顶层窗口自动加入隐藏集合；不符合规则的普通弹窗保持可见。
_避免_：无条件隐藏目标程序的一切弹窗

**配置捕获**：
用户从当前运行中的程序或窗口列表中选择目标，工具自动生成稳定的窗口匹配规则并立即保存。
_避免_：要求用户手工填写窗口句柄或 JSON

**自包含交付**：
用户下载单个 exe 即可运行，不需要预先安装 .NET；安装包只是可选的便捷分发形式。
_避免_：必须先安装运行时才能启动

**支持平台**：
首发支持 Windows 10 22H2 及以上版本和 Windows 11 的 x64 桌面环境。
_避免_：首发承诺 Windows 7/8、32 位或 ARM64

**默认老板键**：
新建受控程序或窗口规则时建议使用 Alt+D 作为初始切换快捷键；用户可以修改，多个目标也可以共用。
_避免_：把默认快捷键视为不可修改

**恢复全部**：
独立于各个切换快捷键的一键兜底操作，用于展示当前所有被隐藏的受控窗口。
_避免_：只能逐个目标恢复

**界面语言**：
设置窗口和托盘菜单默认使用简体中文，并保留切换到英文的能力。
_避免_：首发只有英文界面

**用户配置存储**：
配置和日志存放在当前用户的 AppData 目录，exe 所在目录不承担可写数据职责。
_避免_：默认写入安装目录或要求管理员权限保存配置

**异常恢复**：
正常退出时恢复隐藏窗口；目标程序退出时清理失效记录；工具下次启动时尽力恢复仍存在的上次隐藏窗口。
_避免_：首版引入独立 watchdog 进程

**目标托盘图标**：
目标程序自己的系统托盘图标不属于老板键隐藏范围；工具只控制目标窗口及其桌面入口。
_避免_：首版修改 Windows 通知区域的第三方图标

**工具托盘入口**：
隐藏目标后，工具自身的托盘图标仍然可见，用于设置、恢复全部和退出。
_避免_：把唯一的控制入口也隐藏

**特殊窗口边界**：
普通桌面窗口是首要支持对象；无法稳定隐藏的全屏、受保护或更高权限窗口应被识别并提示，而不是强制操作。
_避免_：为了少数特殊窗口结束进程或绕过权限

**组状态切换**：
共享同一切换快捷键的目标只要有一个仍可见，下一次触发就把整个组统一切换到隐藏；只有全部隐藏时才统一展示。
_避免_：同一快捷键触发后留下半隐藏状态

**标题匹配**：
窗口级规则的标题包含文本匹配默认不区分大小写，以降低程序语言或标题格式变化造成的失配。
_避免_：要求用户精确输入标题大小写

**规则启用状态**：
持久配置可以暂时禁用某个程序或窗口匹配规则，禁用不会删除其快捷键和匹配条件。
_避免_：只能删除后重新配置

**安全删除**：
删除仍有隐藏窗口的规则时，先展示这些窗口，再移除规则，避免用户失去控制入口。
_避免_：删除配置后留下不可见窗口

**安全禁用**：
禁用当前仍有隐藏窗口的规则时，先展示这些窗口，再停止注册该规则的快捷键。
_避免_：禁用规则后留下失去控制的隐藏窗口
